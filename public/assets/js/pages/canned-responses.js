import{api}from'../core/api.js';import{toast,esc}from'../core/ui.js';let edit=null;async function load(){const r=await api('/api/canned');const manual=(r.items||[]).filter(x=>x.source_kind==='manual');document.querySelector('#cannedRows').innerHTML=manual.map(x=>`<tr><td>${esc(x.shortcut)}</td><td>${esc(x.title||'')}</td><td>${esc(x.content)}</td><td><button class="btn danger" data-del="${esc(x.source_id)}">Hapus</button></td></tr>`).join('')||'<tr><td colspan="4">Belum ada canned manual.</td></tr>';document.querySelectorAll('[data-del]').forEach(b=>b.onclick=async()=>{await api(`/api/canned/manual/${encodeURIComponent(b.dataset.del)}`,{method:'DELETE'});load()})}document.querySelector('#cannedForm').onsubmit=async e=>{e.preventDefault();const x=Object.fromEntries(new FormData(e.currentTarget));await api('/api/canned/manual',{method:'POST',body:JSON.stringify(x)});e.currentTarget.reset();toast('Canned Response ditambah');load()};load();
