{
  "name": "agwl5-master-agent-cookie-panel",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/upstream.js && node --check src/security.js && node --check src/db.js && node --check src/report.js && node --check public/app.js",
    "generate:secrets": "node scripts/generate-secrets.js"
  },
  "engines": {"node": ">=20"},
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cheerio": "^1.0.0",
    "connect-pg-simple": "^10.0.0",
    "exceljs": "^4.4.0",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "express-session": "^1.18.2",
    "helmet": "^8.1.0",
    "otplib": "^12.0.1",
    "pg": "^8.16.3",
    "qrcode": "^1.5.4",
    "zod": "^4.1.5"
  }
}
