import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const app=fs.readFileSync(new URL('../public/app.js',import.meta.url),'utf8');const html=fs.readFileSync(new URL('../public/index.html',import.meta.url),'utf8');const css=fs.readFileSync(new URL('../public/app.css',import.meta.url),'utf8');const server=fs.readFileSync(new URL('../src/server.js',import.meta.url),'utf8');const upstream=fs.readFileSync(new URL('../src/upstream.js',import.meta.url),'utf8');
test('deposit and withdraw both expose clean nominal copy',()=>{assert.match(app,/amountDigits\?copyButton\(amountDigits,'COPY NOMINAL'\)/);});
test('result/pasaran UI and mobile layout exist',()=>{assert.match(html,/data-page="result"/);assert.match(html,/PREDIKSI/);assert.match(html,/NOMOR KELUAR/);assert.match(html,/HITUNG \/ NAIKKAN/);assert.match(css,/@media\(max-width:760px\).*result-grid/s);});
test('market API validates period and upstream discovers market forms',()=>{assert.match(server,/MARKET_PERIOD_CHANGED/);assert.match(server,/MARKET_\$\{p\.data\.action\.toUpperCase\(\)\}/);assert.match(upstream,/async markets\(\)/);assert.match(upstream,/async marketAction/);assert.match(upstream,/MARKET_NUMBER_FIELDS_NOT_FOUND/);});
